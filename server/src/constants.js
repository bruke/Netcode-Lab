/** Shared simulation constants (must match client demos). */
module.exports = {
    CANVAS_W: 800,
    CANVAS_H: 600,
    PLAYER_SIZE: 28,
    SPEED_PER_TICK: 5,
    TICK_MS: 50,
    MAX_PLAYERS: 16,
};
